package cmd

/*
func TestExecList(t *testing.T) {
	pwd, _ := os.Getwd()
	os.Setenv("HOME", pwd+"/tests")

	buf := &bytes.Buffer{}
	expected := "test\n"
	if err := execList(buf); err != nil {
		t.Errorf("expected %v to eq %v", err, nil)
	}

	if buf.String() != expected {
		t.Errorf("expected %v to eq %v", buf, expected)
	}
}
*/
